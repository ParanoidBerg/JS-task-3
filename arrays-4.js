let arr = ["JavaScript", "Java", "Python", "Ruby", "C#", "C++", "С", "Go", "Fortran", "PHP"]
let i = 0
while (i < arr.length) {
    if (arr[i].length > 3) {
        console.log(arr[i])
    }
    i++
}
